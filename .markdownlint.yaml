MD013:
  line_length: 120
  tables: false        # table cells are exempt
  code_blocks: false   # code blocks are exempt
MD024: false           # "Strengths"/"Weaknesses" repeat per section intentionally
